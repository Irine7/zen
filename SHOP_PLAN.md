# План реализации: Модуль Экономики, Магазина и Инвентаря 🪴🛒

Привет! В рамках этого модуля мы создадим полноценную систему экономики для **ZenGarden**. Это не просто добавление кода — это отличная возможность освоить продвинутые и критически важные темы бэкенд-разработки: **транзакции баз данных**, **сложные связи в ORM** и **безопасность GraphQL мутаций**.

Этот план разбит на **5 последовательных шагов (уроков)**. Мы будем двигаться строго по очереди:
1. Я объясняю теоретическую часть шага и даю темы для изучения.
2. Ты изучаешь материалы, задаешь вопросы, если что-то непонятно.
3. Я даю тебе конкретное практическое задание с инструкциями.
4. Ты пишешь код, мы его проверяем, и только после этого переходим к следующему шагу!

---

## 🗺️ Карта нашего пути (5 Шагов)

### 📂 Шаг 1: Обновление Базы Данных (Prisma Schema & Migrations)
**Цель:** Спроектировать и создать таблицы для магазина семян и инвентаря пользователей.
*   **Теория для изучения:** Отношения "Один-ко-многим" (One-to-Many) и "Многие-ко-многим" (Many-to-Many) через связующие таблицы в Prisma. Уникальные составные индексы (`@@unique`).
*   **Что сделаем:** 
    *   Создадим модель `Seed` (доступные семена в магазине: Сакура, Бамбук, Сосна, Клен).
    *   Создадим модель `InventoryItem` (инвентарь пользователя, где хранится количество купленных семян).
    *   Запустим Prisma-миграцию, чтобы применить изменения в PostgreSQL.

### ⚡ Шаг 2: Наполнение Магазина (Database Seeding)
**Цель:** Научиться автоматически наполнять базу данных начальными (seed) данными.
*   **Теория для изучения:** Зачем нужен seeding базы данных, как написать скрипт инициализации данных в Prisma.
*   **Что сделаем:** 
    *   Напишем скрипт, который при запуске автоматически добавляет в таблицу `Seed` 4 типа семян с их ценами, названиями и описаниями.
    *   Запустим этот скрипт и проверим базу в **Prisma Studio**.

### 🔍 Шаг 3: GraphQL Queries — Витрина Магазина и Инвентарь
**Цель:** Создать GraphQL схемы и Queries, чтобы фронтенд мог запрашивать товары и инвентарь.
*   **Теория для изучения:** Декораторы `@ObjectType()`, `@Field()` в TypeGraphQL. Создание Query-резолверов с проверкой авторизации (`@Ctx() ctx`).
*   **Что сделаем:**
    *   Напишем GraphQL-сущности (Entities) для `Seed` и `InventoryItem`.
    *   Создадим запрос `getSeeds` (показывает все семена в магазине).
    *   Создадим запрос `getInventory` (показывает купленные семена текущего авторизованного пользователя).

### 💳 Шаг 4: Мутация покупки и Prisma Transactions (Самая важная часть!)
**Цель:** Реализовать абсолютно безопасную покупку семечка с проверкой баланса очков.
*   **Теория для изучения:** Что такое транзакции в БД, свойство **ACID (Atomicity)**. Почему обычные последовательные запросы к БД опасны в финансовых операциях (проблема race conditions). Как использовать `prisma.$transaction`.
*   **Что сделаем:**
    *   Создадим GraphQL мутацию `buySeed(seedId: String!)`.
    *   Напишем логику покупки внутри `prisma.$transaction`:
        1. Проверяем, авторизован ли пользователь.
        2. Ищем семечко в базе и проверяем баланс Zen Points пользователя.
        3. Если очков хватает — списываем очки у `User` и добавляем/увеличиваем количество (`quantity`) этого семени в `InventoryItem`.
        4. Если очков мало или семечка не существует — транзакция прерывается (rollback), и баланс пользователя остается в безопасности.

### 🌱 Шаг 5: Посадка дерева из инвентаря
**Цель:** Переписать/создать логику посадки Bonsai так, чтобы пользователь мог сажать только те семена, которые реально есть у него в инвентаре.
*   **Теория для изучения:** GraphQL Input Types, логика списания ресурсов из инвентаря.
*   **Что сделаем:**
    *   Создадим мутацию `plantBonsaiFromInventory(seedId: String!, habitId: String!)`.
    *   При вызове:
        1. Проверяем, есть ли у пользователя в инвентаре семечко с `seedId` (количество > 0).
        2. Уменьшаем количество на 1 (если стало 0 — удаляем запись из инвентаря).
        3. Создаем новое дерево `Bonsai` выбранного типа, привязанное к привычке `habitId` и пользователю.

---

## 🛠️ Как мы будем работать прямо сейчас

Я буду вести тебя строго по шагам. 

Мы начинаем с **Шага 1: Обновление Базы Данных (Prisma Schema & Migrations)**. 

### Твоя задача на сейчас:
1. Прочти этот план.
2. Напиши мне: **«Я готова начать Шаг 1!»**
3. И я сразу же вышлю тебе первую порцию теории про связи в базах данных и пошаговые инструкции, как изменить файл `schema.prisma`. 

Поехали! 🪴🚀

Урок 5: Посадка дерева из инвентаря 🌱
Сейчас в нашей игре есть логическая дыра: пользователь может вызвать мутацию createBonsai и посадить дерево просто так, даже если у него в инвентаре пусто.

Мы перепишем этот процесс. Вместо createBonsai мы сделаем мутацию plantBonsaiFromInventory:

Пользователь передает seedId (какое семечко посадить) и habitId (к какой привычке его привязать).
Бэкенд проверяет, есть ли это семечко в инвентаре у пользователя.
Если есть:
Списывает 1 единицу семечка (если количество становится 0 — полностью удаляет запись из инвентаря, чтобы не засорять базу).
Создает новое дерево Bonsai соответствующего типа (PINE, SAKURA, MAPLE, BAMBOO).
Если семечка нет — возвращает ошибку.
📚 Теория: Безопасное списание ресурсов в Prisma
Поскольку списание семечка и создание дерева — это связанные операции, они обязаны происходить в одной транзакции (prisma.$transaction).

Если мы спишем семечко, но создание дерева по какой-то причине упадет (например, эта привычка уже привязана к другому дереву, так как поле habitId уникально в модели Bonsai), транзакция откатится, и семечко вернется обратно в инвентарь!
🛠️ Практическое задание
Давай добавим типы ошибок, а затем реализуем мутацию в резолвере.

Шаг 5.1: Добавим ошибку отсутствия семечка в инвентаре
Открой файл ошибок 👉 

errors.ts
 и добавь в конец класса новый тип ошибки:

typescript
@ObjectType()
export class SeedNotInInventoryError {
  @Field(() => String)
  message!: string;
}
Не забудь добавить этот класс в BuySeedResult? Нет, эта ошибка относится к процессу посадки дерева. Для посадки мы сделаем отдельный Union Type: PlantBonsaiResult.

Шаг 5.2: Обновим bonsai.resolver.ts
Давай посмотрим, где у нас создаются деревья. Это файл 👉 

bonsai.resolver.ts
.

Мы:

Зарегистрируем Union Type PlantBonsaiResult (успешный Bonsai или ошибка SeedNotInInventoryError).
Добавим мутацию plantBonsaiFromInventory.
Подключим проверку инвентаря и логику транзакции.
Открой 👉 

bonsai.resolver.ts
 и давай внесем изменения.

Сначала импортируем SeedNotInInventoryError из модуля shop. Добавь в импорты наверху:

typescript
import { SeedNotInInventoryError } from '../shop/errors';
Затем создадим PlantBonsaiResult (добавь после BonsaiResult на 20 строке):

typescript
export const PlantBonsaiResult = createUnionType({
	name: "PlantBonsaiResult",
	types: () => [Bonsai, SeedNotInInventoryError] as const,
});
И заменим старую мутацию createBonsai на новую безопасную логику. Найди в файле метод createBonsai (он находится примерно на 106–120 строках):

Старый код:

typescript
// Создаем дерево
	@Mutation(() => Bonsai)
	async createBonsai(
		@Arg("input", () => CreateBonsaiInput) input: CreateBonsaiInput,
		@Ctx() ctx: Context
	): Promise<Bonsai> {
		if (!ctx.userId) {
			throw new GraphQLError("Вы не авторизованы!", { extensions: { code: 'UNAUTHENTICATED' } });
		}
		return prisma.bonsai.create({
			data: {
				...input,
				userId: ctx.userId
			}
		});
	}
Замени его на новый метод:

typescript
// Посадка дерева из инвентаря
	@Mutation(() => PlantBonsaiResult)
	async plantBonsaiFromInventory(
		@Arg("seedId", () => String) seedId: string,
		@Arg("habitId", () => String) habitId: string,
		@Ctx() ctx: Context
	) {
		const userId = ctx.userId;
		if (!userId) {
			throw new GraphQLError("Вы не авторизованы!", { extensions: { code: 'UNAUTHENTICATED' } });
		}
		return prisma.$transaction(async (tx) => {
			// 1. Проверяем, есть ли семечко в инвентаре пользователя
			const inventoryItem = await tx.inventory.findUnique({
				where: {
					userId_seedId: {
						userId,
						seedId
					}
				},
				include: { seed: true }
			});
			if (!inventoryItem || inventoryItem.quantity <= 0) {
				return Object.assign(new SeedNotInInventoryError(), {
					message: "У вас нет этого семечка в инвентаре!"
				});
			}
			// 2. Списываем семечко из инвентаря
			if (inventoryItem.quantity === 1) {
				// Если было последнее семечко — удаляем запись
				await tx.inventory.delete({
					where: { id: inventoryItem.id }
				});
			} else {
				// Если больше одного — уменьшаем количество на 1
				await tx.inventory.update({
					where: { id: inventoryItem.id },
					data: { quantity: { decrement: 1 } }
				});
			}
			// 3. Создаем Bonsai дерево
			// Тип дерева (PINE, SAKURA и т.д.) берется из связанной модели Seed!
			const newBonsai = await tx.bonsai.create({
				data: {
					type: inventoryItem.seed.type,
					userId: userId,
					habitId: habitId,
					lastWateredAt: new Date()
				}
			});
			return Object.assign(new Bonsai(), newBonsai);
		});
	}
🎯 Твое действие:
Добавь SeedNotInInventoryError в 

errors.ts
.
Замени createBonsai на plantBonsaiFromInventory в 

bonsai.resolver.ts
.
Сохрани файлы и проверь, что TypeScript компилирует код без ошибок.
Отпишись мне о результатах, и мы разберем, как это протестировать в Apollo Studio Sandbox! 🪴🚀

